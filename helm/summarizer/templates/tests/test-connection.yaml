apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "summarizer.fullname" . }}-test-connection"
  labels:
    {{- include "summarizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "summarizer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
